
# TODO test for http://bugzilla.nanofluidics.com/show_bug.cgi?id=26048

import logging
import json
import os.path as op

from pbcommand.pb_io.report import load_report_from_json
from pbcommand.models import FileTypes

from pysiv2.custom.base import TestReportStatistics

log = logging.getLogger(__name__)
logging.basicConfig(level=logging.DEBUG)

class TestIsoSeqClassify(TestReportStatistics):
    """
    Test the output of the ``isoseq_classify`` report.
    """
    REPORT_ID = "isoseq_classify"
    TEST_ID = "isoseq_classify"
    METRIC_IDS = [
        "num_reads",
        "num_5_seen",
        "num_3_seen",
        "num_polya_seen",
        "num_flnc",
        "num_fl",
        "num_nfl",
        "avg_flnc_len",
    ]
