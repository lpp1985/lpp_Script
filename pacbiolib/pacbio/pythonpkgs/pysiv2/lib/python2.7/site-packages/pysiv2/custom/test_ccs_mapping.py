
"""
Tests specific to CCS+Mapping jobs.  Note that the mapping statistics report
is covered by test_mapping_stats.
"""

from unittest import SkipTest

from pbcommand.models import FileTypes
from pbcore.io import GffReader

from pysiv2.custom.base import (TestFilesWereGenerated,
                                TestReportStatistics)


class TestReportOutput(TestFilesWereGenerated):
    """
    Check for the presence of specified task output files.
    """
    TASK_IDS = ["pbreports.tasks.summarize_coverage_ccs",
                "pbreports.tasks.mapping_stats_ccs",
                "pbreports.tasks.coverage_report"]
    FILE_TYPES = [FileTypes.GFF, FileTypes.REPORT, FileTypes.REPORT]
