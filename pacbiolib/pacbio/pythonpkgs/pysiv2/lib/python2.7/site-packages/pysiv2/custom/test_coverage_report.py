
from pysiv2.custom.base import TestReportStatistics

class TestCoverageReport(TestReportStatistics):
    """
    Test attribute values in the `coverage` report against expected values.
    """
    REPORT_ID = set(["coverage", "coverage_hgap"])
    TEST_ID = "coverage"
    METRIC_IDS = ["depth_coverage_mean", "missing_bases_pct"]
    DEFAULT_VALUES = {"missing_bases_pct":0}
